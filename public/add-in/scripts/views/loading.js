(function() {
    "use strict";

    let LoadingView = function () {

        let initialize = function (view, data) {
            
        };

        this.initialize = initialize;
        this.templateSelector = ".loading.view";
    };

    window.PollParty.Views.LoadingView = LoadingView;

})();